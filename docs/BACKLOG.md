> 共有スナップショットの参照資料。元ホストの記録であり、実行・再開・公開の指示ではありません。未収録の証拠へのリンクを含みます。共有範囲は `docs/team/SHARING_SCOPE_JA.md` を参照してください。

# Backlog

## User-requested handoff (separate from scientific work)

Prepare the system/context team snapshot and verify its portable subset. New
GitHub repository creation requires the user's empty-repository URL after the
existing credential returned403 on creation. No system upload completed yet.
Do not replace this request with autonomous new experiments; preserve Plan40's
single worker/claim. See D-086 and `docs/team/START_HERE_JA.md`.

## Now — Follow the single running Plan40 diagnostic

1. Registration and independent audit PASS; Plan39 complete/archived. Frozen
   inputs and hashes: `experiments/reports/0040-registration-ja.md`. Do not repeat
   the sketches or preregistration. No code/games in the finished registration unit.
2. Six-file implementation, final49focusedtests/source audit and full regression
   accepted:1552tests/5382.233s/OK(skip2)/actualexit0. Saved-evidence audit, old206/
   new6 pins and runner acceptance gate PASS. `0040-technical-acceptance-ja.md` and
   `0040-full-suite-exit-receipt.json` under experiments/reports are authoritative.
   Regression finished; never poll/restart54037 or consume the third focused run.
   Ledger focused2/3, full1/1, claim1/1,16games scheduled; sources remain frozen.
3. Preflight/sealed-input audit PASS; one worker RUNNING session29306,
   PythonPID29459, observed11:43:48Z. Claim and saved registration confirmed;
   0game records saved at observation, no completion. Resume this worker once on
   next wake, never reseal/relaunch. Log/exit: `diagnostic-worker.log`/`.exit` in
   `experiments/reports/plan0040-acceptance-evidence/`. CPU≤14,400s/output≤16MiB,
   decision≤32,768transitions/depth3/game≤3m. No extra seeds, board/resource
   tuning, initial exact query or retry. Saved-only review reports actual depth,
   UNKNOWN, role/first-player/policy split, and limits; all quality flags false.
4. The one cohort and information review end this unit. No automatic UI/deploy,
   public GitHub modification, new candidate or replacement batch.

## Boundaries and already-finished work

D-055 no draws; D-080 no placement-exhaustion loss or semantic relabeling.
Nonasymmetry/undefined endings fail the gate; computational UNKNOWN is not a loss.
Plan39 used2sketches/1design review; only「架橋と転色」selected, not human-ready.
The courier draft is rejected by a simple B strategy; do not repair/research it.
Plan38 episode3 cancelled; all old workers/queries finished, no polling/restarts.
Preserve protected Plan13/14, frozen fixtures/runs,206Python pins, old public
login-free game and six-file GitHub export. No payments/recruitment/model per move.
Hourly `parity-forge` remains active and quiet on unchanged status.

Complete former backlog is in [the checkpoint](BACKLOG_2026-09-10_CHECKPOINT.md).
Historical next-action statements there do not reopen old work.
