> 共有スナップショットの参照資料。元ホストの記録であり、実行・再開・公開の指示ではありません。未収録の証拠へのリンクを含みます。共有範囲は `docs/team/SHARING_SCOPE_JA.md` を参照してください。

# Historical backlog and preservation commitments — 2026-09-05

Preserved before shortening the live backlog. Historical checkpoint statements
are not current tasks. Frozen-artifact preservation constraints remain binding;
use [`BACKLOG.md`](BACKLOG.md) for current priorities.

## Now

Finish the existing full regression and Plan-0015 negative closeout described
under **Next**. The commitments below are preservation constraints, not a queue
to repeat old implementation or experiment work.

- Preserve Plan 0008 as formally `INCONCLUSIVE`; do not evaluate its 56
  unselected stress-eligible cases or promote its descriptive `ROLE_DIVERSE`
  sample.
- Preserve Plan 0009 as `NOT_SUPPORTED_VECTOR_BOUNDARY`; do not reinterpret it as
  overcorrection or a dominance cliff, extend its sample, or generate toward
  capture.
- Preserve Plan 0010 as `NOT_SUPPORTED_TWO_RUNNER_SETUP`; do not add runners,
  extend its sample, relax candidate shape, or combine the setup with capture or
  stalemate draw.
- Preserve Plan 0011 as `SUPERSEDED_PRE_OUTCOME`. Keep its reviewed census,
  selector, state proof, exact evaluator, depth-5 evaluator, frozen roots,
  schedules, thresholds, seeds, caps, and 367-test checkpoint. No production
  Plan-0011 manifest, label, profile, run, reservation, attempt, failure, lock, or
  outcome exists; do not infer a transfer result.
- Preserve the reviewed Plan-0012 slice-1 family identity and 381-test checkpoint.
  Keep final six-cell membership, search domains, and final derivation roots
  unfrozen until schema-v4 feasibility exists. Add a provisional reconstructable
  derivation witness after the v4 compiler and before the outcome-free census.
- Preserve the reviewed schema-v4 terminal/PUSH, SWAP, HOP, and CONVERT
  checkpoints. Keep schemas v1–v3 byte- and behavior-identical, v4
  PUSH/SWAP/HOP/CONVERT definitions parser-normalized, and all fixed DSL/D4
  identities unchanged. Preserve the pre-implementation finding that proposed
  `CAPTURE_STEP` aliases existing `MOVE_CAPTURE`; do not add or count that alias,
  and use `MOVE_CAPTURE` in the provisional capture cells. Require future active
  compiler and final-registry builders to cross the explicit nonretired-family
  allow-list boundary before emission.
- Preserve the reviewed schema-v4 `ELIMINATE` goal: no opponent-owned target
  kind remains; no edge/counter/history field; initial zero targets are a
  deliberate trivial goal for later feasibility to exclude. Keep its v4-only
  parser/direct-construction boundary, admission rule, D4 non-spatial treatment,
  static relaxation, simplicity accounting, and goal-progress-v1 fail-closed
  agent boundary fixed.
- Preserve replay telemetry v1 as descriptive evidence rebuilt only from an
  authoritative schema-v4 definition and complete retained trace. Keep its
  closed vocabularies, exact one-ply counterfactuals, separated successor/effect/
  outcome/next-legal-set measures, strict hostile-input boundaries, work caps,
  and no-score/no-policy contract fixed. Preserve all seven synthetic fixtures,
  their definition/trace/evidence digests, all-fixture D4 checks, the SWAP-cycle
  role-swap check, and ordered benchmark root
  `6789f97e7f0948df9fa66f75933c061a08e029ce49e9d9da33ae57a7b4a59a33`.
  Do not reinterpret this calculation benchmark as gameplay evidence or a
  calibrated strategic-quality threshold.
- Preserve the provisional feasibility-domain/compiler v1 checkpoint: 214,368
  unique ordered inputs, domain hash
  `3456b5873dadfd177639b4c0d062bad6717d328a7ebee88a71f4294f9a5b0bc5`,
  ordered input root
  `e3011f74256fe39881eda3636533ec4352f7b4d2c4386e12d775ccc031b6d2e8`,
  six exact compiler projections, derivation-witness reconstruction, and the
  four action-specific structural-bound classes. Treat it as a provisional
  census input, not a final registry, search envelope, or atlas.
- Preserve the outcome-free feasibility census and digest
  `ade64ebaaf9892848e03c2312786a6ef3825f3c82f4311e16a9c810711d035d2`.
  It records 71,384/24,208 eligible exact/D4 cases, all six families, 144
  supported strata, complete rejection-reason combinations, case-to-D4 and
  orbit-witness roots, 1,714,944 invariant transform checks, and 391 public-path
  identity checks across all 176 strata. Do not reinterpret structural support
  as dynamic reachability, fairness, strategic quality, or fun.
- Preserve Plan 0013's outcome-free selector checkpoint at `5bce4c0`. Keep the
  six-family registry, 3+3 envelope, 2+2 deferral proof, cutoff-fixed 66-blob
  history projection, seven-fixture synthetic projection, exact A/B pairing,
  eight-orientation witnesses, and roots unchanged. The frozen partition is
  8,854 pre-history pairs, zero collisions, 144 development pairs/288
  definitions, 144 disjoint candidate-block pairs/288 definitions, and 8,566
  untouched pairs/17,132 definitions. Do not evaluate, replenish, replace, or
  consume the candidate block during development.
- Preserve Plan 0013's family-neutral evaluator checkpoint at `d34c383`. Keep
  the five-fixture registry (four exact Plan-0012 reuses plus one new disjoint
  SWAP/CONNECT fixture), 40 D4 exact/search slots, 320 random and 320
  terminal-only lifecycle games, exact root/recursive B-minimization witnesses,
  role-slot seed reuse, fixed roots, and zero production access/outcome claims.
  The authorized initial ladder is only `random-v1-weak` and
  `terminal_only_minimax-v1-depth1`, with the 3,456-node per-role-slot cap;
  depth 2 remains diagnostic only.
- Preserve Plan 0013's current outcome-free V2 protocol and six-runner checkpoint
  at `0697800`. Keep the six-stage order, every exact/orientation/profile/role/
  game/matched/telemetry coordinate and root, all work caps, exact and weak-
  sampled labels, descriptive family-signal rule, and inspection selection
  unchanged from the initial design checkpoint `28e6a03`. Its V1 execution
  envelope was superseded before any manifest or outcome; do not restore it.
  The current protocol root is `8126c33f…f04c`, its 2,220,598 canonical bytes
  have SHA-256 `b732f174…9229`, and the evidence store is V2.
- Preserve all six independent `run`/`recover` entrypoints, their recursively
  authenticated production closures, candidate-neutral pre-reservation
  bootstrap, immutable per-slot journals and full ledgers, exclusive terminal
  lifecycles, recursive fixed-parent authentication, contradiction sidecars,
  completion gates, and crash recovery at `0697800`. Outcome stages must use
  protocol-only schedules and detached definitions, never the full selection or
  candidate block. The focused 25/25 and 109/109 runs and complete 769/769 suite
  passed; no production evidence directory or outcome exists.
- Preserve the completed outcome-free Plan-0013 manifest at bootstrap root
  `96c7e239…ea22`, manifest root `ffbb2256…f7c3`, canonical blob SHA-256
  `d3eb19a3…ee89`, and terminal seal `5ee92d2b…e46`. It binds 144 pairs/288
  definitions/2,304 orientations, evenly split 24/48/384 per family, with zero
  candidate-block overlap/export and zero gameplay outcomes. Three independent
  audits found no P0–P3 defect and focused tests passed 72/72.
- Preserve the source provenance captured by all six terminal seals at HEAD
  `5e6a865` and tree `17468fd2…fad`. The temporary fixed-HEAD execution condition
  ended when the sixth terminal was sealed. This transition archives the
  complete V2 store and documentation together, excluding only `.locks/*.lock`
  as regenerable operational state rather than evidence.
- Preserve the completed exact stage at terminal `6c575e50…a6c45`, summary root
  `da72fa33…1cc5`, ledger SHA-256 `9724f7ec…9836`, and 2,592-record catalog
  root `bceb6e87…920e`. Exact is 288/288 `COMPLETE`, D4 PV is 2,304/2,304
  `VALID`, and 2,538,786 states were searched without invalidity or proof
  contradiction. Raw A/B/draw is 98/179/11; pair labels are 51 first-player,
  23 A-role, 62 B-role, zero second-player, and eight horizon affected. Exact-
  gate survival is 51/144 pairs in 31/44 strata; do not reinterpret it as a
  fairness, quality, or final-candidate result.
- Preserve the completed random stage at terminal `bafc4a52…e9b6`, summary root
  `5e268d12…34da7e`, ledger SHA-256 `88e00f44…cf0f`, and catalog root
  `dbb28915…cdf`. All 18,432 games are complete, raw A/B/draw is
  7,142/9,470/1,820, and the fixed random gate retains 26/144 pairs. Its exact-
  gate intersection is 16 pairs across 12 strata, all exact first-player-
  dominant; do not reinterpret them as fair single games or final candidates.
- Preserve the completed terminal-only depth-1 stage at terminal
  `f5d1e937…be226`, summary root `4e2e53cf…a105`, ledger SHA-256
  `33f65a16…680f`, and catalog root `77e1fa7c…21fe`. All 18,432 games are
  complete with zero node censor; A/B/draw is 6,380/11,806/246 and node use is
  279,242. The exact/random/depth-1 intersection is 14 pairs across 10 strata,
  all exact first-player-dominant; it is a two-game role-swap frontier signal.
- Preserve the completed telemetry stage at terminal `2869fc55…f670`, summary
  root `a69a8cc1…97a7`, ledger SHA-256 `429ef172…a6a3`, and catalog root
  `6a91ae0c…d8d7a`. All 36,864 complete traces are validated; all exception
  counts are zero. Telemetry is descriptive and must not alter any formal pair
  or family gate.
- Preserve the failed Plan-0013 V2 assessment at terminal `812b8c4b…e889`,
  reservation `91138600…a6d`, attempt `bffac6f2…9c96`, failure-body SHA-256
  `9db7d724…385d`, and closure `f7792895…b2e4`. It has no partial evidence,
  completed body, formal report, or inspection artifact. Do not rerun, replace,
  delete, or reinterpret it as a negative gameplay result.
- Treat the 14-pair/10-stratum exact/random/depth-1 intersection as a raw
  parent-evidence diagnostic only. All 14 are exact first-player-dominant. Do
  not assign formal family statuses or allocate the confirmation candidate
  block from it.
- Preserve Plan-0014 slice 1 as a two-defect-only, read-only reconstruction.
  Keep the core/lifecycle split, descriptor-bound no-create V2 reader, path-race
  defenses, fixed report root `2a873b0b…5484d`, inspection SHA-256
  `0fc52593…03c7e`, 541.990-second real-parent test, and P0-P3-free independent
  review. These values are now embedded unchanged in the separately versioned
  Plan-0014 attestation and still do not complete Plan 0013.
- Preserve the slice-1 boundary suite result: 799 dependency-free tests passed
  in 1,044.068 seconds; the sole skip was the opt-in real-V2 test already run
  separately above.
- Preserve `37d95eb…d58ee` / tree `87e47004…46db` only as the rejected initial
  Plan-0014 implementation checkpoint. Its protocol root `5424b2d0…f8458` and
  SHA-256 `adb409ba…fa88` were never authorized or invoked because independent
  pre-run audits found one P2 blocking-FIFO reader defect. Do not use or erase
  this negative implementation evidence.
- Preserve reader-hardening checkpoint `bcec701…ed057` / tree
  `69553e07…4cb0`, its exact five-production/four-test change surface, and its
  five historical blob pins. This is non-scientific type/liveness hardening:
  zero new assessment corrections and zero scientific-rule changes. Preserve
  its 80 focused tests and 873-test full boundary in 1,102.015 seconds.
- Preserve the hardened Plan-0014 report-only protocol at root
  `b412e0af…5d38d`, 16,880 canonical bytes, SHA-256 `0aeb2bf4…cfd1`, exact
  13-path recursive allow-list, three repair-production equivalence pins, four
  reader-hardening production pins, six unchanged scientific-dependency pins,
  and all capability exclusions. The final protocol module is bound by the
  final production closure rather than its pre-self-reference hardening blob.
- Preserve the real Plan-0014 source-binding identity `f763bad5…e5157` and
  archive/live inventory root `773941af…d612` over 152,680 tracked files and
  601,787,372 bytes. Operational locks and the empty contradiction directory
  remain outside that identity but must pass their exact metadata contracts when
  present.
- Preserve the historical 765.274-second read-only source/report/attestation
  reconstruction, 68 focused tests, and 867-test boundary, but do not treat the
  superseded checkpoint's earlier zero-finding review as final approval. Keep
  the operational-lock, child-to-parent fsync, and special-file regressions.
- Preserve the final Plan-0014 source `3fa9f6b…3f992`, tree `f491a361…6a50`,
  closure `975541c3…57d7`, and one-shot evidence terminal `9272adcd…b5ae`.
  Public recovery is `VERIFIED_NO_OP`; all three pre-run and all three post-run
  audits reported P0=P1=P2=P3=0. The inner report `2a873b0b…5484d` and outer
  attestation `9ca2d719…5290` preserve the original V2 `FAILED` lifecycle, two
  integration repairs, zero scientific changes, and zero candidate/game/solver
  generation. Do not call the two positive family-count signals fair games or
  confirmation.
- Preserve the Plan-0013 candidate block as unallocated and unevaluated. Plan
  0015 excludes all six public semantic regions and their role-swapped images
  without opening that artifact.
- Preserve Plan-0015's sealed 109 fresh semantic classes and 1,518 canonical
  profiled skeletons as structural identities, not game-quality results. The
  one authorized 5,111,055-carrier census is `COMPLETED`, public recovery is
  `VERIFIED_NO_OP`, and its 880,986 representative / 2,025,808 weighted eligible
  supply remains structural evidence rather than a candidate or quality result.
- Preserve Plan-0015 slice 1 as the sealed, outcome-free grammar checkpoint.
  Keep the standalone `parity_forge_universe` package capability-minimal, the
  frozen legacy initializer byte-identical, all semantic/spatial/profile/old-six
  authorities code-derived or fail-closed, and descriptor root
  `05826dc2da02890f3f562ee2d6febda523c58837affb757b5dff6d62c074e6ab`
  fixed. Preserve its 47/47 dedicated and 921/921 complete test boundaries and
  P0-P3-free final reviews. Do not reinterpret its 109 fresh classes or 1,518
  skeletons as feasible setups, candidates, games, or quality evidence.
- Preserve Plan-0015 slice 2 as the strict canonical-wire compiler checkpoint.
  Keep compiler/test blobs `95bb7581…251a` / `d796545f…3113`, the complete
  source inverse, D4/owner/first-player/complete-role transforms, nested
  method-shadow defenses, 35/35 focused boundary, and 956/956 complete boundary
  fixed. Production must remain free of the legacy package and every transition,
  terminal, outcome, solver, agent, telemetry, selection, candidate, filesystem,
  network, and dynamic-import capability.
- Preserve Plan-0015 slice 3's completed one-carrier `initial_structure` kernel.
  Keep source/test blobs `c7d806aa38c3a8152ae6289f64e3cba0765a7ac1` /
  `18ff527411db6fc1669c68358f8435425ed7287b`, exact initial goals and
  legal/dependency counts, pair-aware support, resource-aware A/B-first count
  lattice, work bounds, structured reasons, separated descriptor groups, and
  strict prepared snapshot boundary fixed. Preserve the 30/30 focused result in
  9.618 seconds, the 989/989 complete result in 4,357.623 seconds with two
  intended skips, and both independent P0-P3-free final audits. Do not
  reinterpret static eligibility as dynamic reachability, fairness, strategy,
  replay value, or fun.
- Preserve Plan-0015 slice 3's factorized `static_census` implementation at
  source/test blobs `fd5fb486ea33fac840b2951ff0a3680a3c221058` /
  `93aa6d428b5378f06027438c04d6a38f9ad25bae`, setup-table root
  `f1bc82d1cfbaac9933e097aba2f4118737e19f7a190f06bc9f0a83add17c2e23`,
  and skeleton-authority root
  `53221ffdbd42e4c86e9c54eadb6e0659bf71ff350dcdf0944b062ce56c3e1488`.
  Preserve the 36/36 dedicated result in 601.045 seconds, 1,025/1,025 complete
  result in 4,908.329 seconds with two intended skips, both P0-P3-free final
  audits, exact nested-type boundaries, and one-shot linear derivation path.
  The synthetic report is only an aggregation test double; do not infer a
  production census, eligible carrier, candidate, fairness, strategy, or fun.

## Next

- Finish the running full regression of the repaired, standard-library-only
  history adapter. Its 33 focused tests and independent final review are clean.
  Preserve the 1,179 exact / 1,175 D4 / 1,175 role-neutral census and corrected
  `00973dcc…e2e3c9` projection root, including complete role and global piece-kind
  alpha renaming. All 66 legacy blobs and seven fixture wires are mandatory.
- Close Plan 0015 as `BREADTH_FLOOR_NOT_MET`, with partition `NOT_STARTED`.
  The authenticated census has nine all-zero semantic classes; historical
  exclusion can only reduce supply, so ranking cannot satisfy all-109 support.
  Record the explicit post-proof decision to omit partition; do not pretend it
  was executed or backfill from the other 100 classes.
- Define and independently review the next study's general scope, exclusion,
  and coverage criteria before new selection. Distinguish all-class support,
  complete supported/rejected accounting, and game quality. Do not begin a new
  primitive or a 48-spec removal audit merely because the old universal breadth
  floor failed; its necessity has not been demonstrated.

## Later

- A terminal-blind bounded structural probe only after a later plan supplies an
  independently frozen development set; Plan 0015 supplies none.
- Consider a single missing topology such as `REMOVE_ADJACENT` only after a
  question and falsifiable decision rule justify its definition-level study.
  `SURROUND` remains an alternative, not a simultaneous action-and-goal expansion.
- Exact/sampled evaluation and fresh one-shot confirmation only after an
  outcome-free development portfolio and evaluator protocol are frozen.
- Quality-diversity/evolutionary search and compression only inside a supported
  later frontier, with immutable lineage and novelty preservation.
- Role-specialized agents and a playable human-test interface.
- Human calibration of elegance, comprehension, role satisfaction, and replay
  desire.

## Research questions

- Does the complete current isotropic occupancy-v4 grammar retain static support
  across all 109 fresh role-neutral semantic classes?
- Can D4-plus-role-swap canonicalization and frame-stabilizer factoring close a
  multi-million-carrier universe without materializing it or hiding rejection
  mass?
- Which transition-blind structural signals, if any, replicate across two setup
  resource conditions without becoming an outcome proxy?
- Can schema v4 remain a strict, deterministic occupancy extension while leaving
  every schema-v1/v2/v3 byte and behavior unchanged?
- What structural support is genuinely shared across heterogeneous actions, and
  what absolute density, branching, state-space, and work differences remain?
- Can a later matched-ablation design localize failure to a ludeme, rather than
  over-interpreting unmatched family comparisons?

## Technical debt

- Experiment provenance assembly is repeated across historical runners. New
  future-facing evidence primitives may consolidate canonical JSON, exclusive
  writes, reservations, failures, byte seals, executable fingerprints, and
  ancestry, but must not refactor or weaken frozen validators.
- Synthetic agency/interaction fixtures can validate calculations and known false
  positives, but human meaning thresholds remain uncalibrated; the first atlas
  keeps telemetry descriptive.
- Goal-directed tie comparison uses exact floats from simple rational operations;
  replace it if future heuristic terms introduce unstable arithmetic.
- Current sampled agents can be orientation-sensitive through coordinate-order
  tie-breaking; a later atlas must prove agent D4-equivariance or use a complete
  predeclared D4 orientation schedule.
- Full feasibility reconstruction deliberately verifies 1,714,944 transformed
  measurements and peaks near 431 MB because it retains 102,336 compact-orbit
  records. This is acceptable for the frozen 214,368-case domain, but a larger
  future domain should stream or externally sort orbit witnesses rather than
  scale this in-memory design.
- Full protocol reconstruction currently materializes a roughly 2.2 MB
  canonical value and peaked near 491 MB during its exhaustive schedule tests.
  This is acceptable for the frozen 288-definition atlas; a materially larger
  future protocol should add a versioned streaming reconstruction rather than
  silently changing this one.
- The old assessment tests did not exercise the production telemetry join or
  zero-legal observation shape; Plan 0014 closed that historical gap with both
  real-parent boundaries. Future terminal stages must retain equivalent real
  parent/container coverage before production.

## Human-dependent

- Later: comparative elegance, comprehension, role satisfaction, and replay
  desire.
- Later: permission before paid compute, publication, or outside playtesting.
