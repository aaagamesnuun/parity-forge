> 共有スナップショットの参照資料。元ホストの記録であり、実行・再開・公開の指示ではありません。未収録の証拠へのリンクを含みます。共有範囲は `docs/team/SHARING_SCOPE_JA.md` を参照してください。

# Historical project-state snapshot — 2026-09-05

Preserved before shortening the live managerial snapshot. Status statements
below describe that checkpoint, not current execution. Use
[`PROJECT_STATE.md`](PROJECT_STATE.md) for current status and the next action.

## Current phase

Phase 15 — closing with `BREADTH_FLOOR_NOT_MET`; full regression acceptance is
pending. Phases
0–11 established the deterministic system and showed that the original
place-versus-move, capture, vector-boundary, and two-runner lanes can create
mechanical interaction but produced no non-dominant candidate shape. Plan 0012
implemented schema v4 and a six-family outcome-free substrate. Plan 0013 froze
and evaluated that hand-selected 3×3 portfolio; its assessment lifecycle remains
`FAILED`, but Plan 0014 separately reconstructed the unchanged preregistered
calculation and sealed it `COMPLETED` at terminal `9272adcd…b5ae`. All 14 formal
frontier pairs are exact first-player-dominant, so the report confirms no fair
standalone game and leaves the Plan-0013 candidate block untouched.

Plan 0015 moved beyond the six chosen signatures. It closed the current
isotropic occupancy-v4 grammar before any outcome: 109 fresh role-neutral
semantic classes, 1,518 asymmetric profiled skeletons, and a completed factorized
3×3 master census. A read-only reconstruction now proves that nine semantic
classes have zero static-eligible supply, so the registered all-109 breadth
floor cannot be met. No partition was executed or allocated. The planned
development probe is not authorized by this result.

## Current objective

Finish regression acceptance of the repaired identity-only history projection
and record the negative Plan-0015 closeout. Preserve all frozen census evidence
and the unopened Plan-0013 candidate block. Define the next independent study's
general coverage and exclusion rules before any selection or mechanism change.

## Current hypothesis

The existing language is broader than the original six cells, but it does not
support every syntactically coherent semantic class under this fixed envelope.
The remaining 100 classes have static supply, not demonstrated game quality.
Failure of universal syntactic-class support does not itself demonstrate a need
for another primitive. Complete accounting of supported and rejected classes,
and eventual game-quality testing, are different scientific questions.

## What is working

- The v1 DSL, immutable engine, structured generators, static/simplicity/asymmetry
  evaluators, seeded play, exact solver, and budgeted depth-5 agent are deterministic
  and inspectable.
- Frozen static run `20260830T153256441950Z-static-4ad81a40` matched 7/7 cases.
- Two 100-definition development batches explain every rejection; a frozen 20-case
  exact corpus contains 11 A wins, 7 B wins, and 2 draws.
- Depth 5 matched all 20 development and all 14 held-out forced directions.
- The one-shot held-out cascade and blind audit preserved clean-commit provenance,
  attempt evidence, deterministic budgets, and immutable raw results.
- Held-out exact solving completed 14/14 cases; depth 5 completed 12/12 selected 4×4
  cases and the 14-case blind audit without node exhaustion.
- The landscape evidence chain separately froze an outcome-free manifest, a raw
  all-case result, and an adaptive draw-stress result with one-shot reservations
  and clean-commit provenance.
- Strict schema v2 represents only the explicit stalemate-draw policy while
  preserving frozen schema-v1 canonical bytes, hashes, prose, and behavior.
- Strict schema v3 adds only B runner capture-on-entry while preserving frozen
  schema-v1/v2 canonical bytes, hashes, prose, and behavior. Its paired manifest,
  replay attestation, raw-response, and adaptive-stress logic have independent
  pre-outcome review and fail-closed one-shot runners.
- Capture pre-outcome implementation and protocol were committed at `c90ec9a` and
  recorded at `e92bc03`; no frozen schema-v3 corpus definition or treatment
  outcome existed at that checkpoint.
- The paired stalemate evidence chain completed from a baseline-informed,
  stalemate-treatment-outcome-free 128-pair manifest: exact 128/128, no horizon or
  state censor, and independently rebuilt raw/stress aggregates and inspection
  selection.
- Stalemate treatment produced 73 A wins, 35 B wins, and 20 natural draws, but only
  two were static-valid; depth 5 made both 30/30 shape-failing stalemates. Frontier
  coverage is complete at zero and the overall treatment result is not supported.
- Exact solving completed 384/384 frozen landscape cases without censoring: 256 A
  wins, 115 B wins, and 13 draws, using 405,771 states in total and at most 2,975.
- Depth 5 completed all 13 exact-draw diagnostics with 412,498 nodes in total, no
  budget censoring, and no decisive misclassification.
- Independent replay reconstructed the outcome-blind manifest, all 384 exact
  results and principal variations, 610 cheap profiles comprising 18,300 games,
  and every aggregate without mismatch.
- Frontier evidence is complete: the sole cheap-shape-clean draw was tested and the
  diagnostic-frontier count is zero.
- The outcome-free capture manifest froze 128 pairs over 32 strata. Fresh source
  replay matched pinned baseline evidence case-for-case and root-for-root.
- Capture exact solving completed 128/128 without state censoring or `PLY_LIMIT`,
  changed 61 A wins to B wins, and met both preregistered response thresholds.
  Treatment A/B was 32/96; 86 exact PVs executed capture and all monotonicity checks
  passed.
- The capture stress run completed all 32 selected profiles without node censoring
  or direction mismatch. Its descriptive frontier was three exact A wins and 29 B
  wins, and every profile was 30–0 role-dominant.
- Public reconstruction and independent evidence audit matched the capture
  manifests, runs, traces, aggregates, selection, provenance, and hashes without a
  P0, P1, or P2 defect.
- All 200 dependency-free tests pass at the completed capture implementation.
- Plan 0009's pre-outcome implementation is frozen at
  `33a735ec0e474de00100bc1adfa67249045097b2`. It reconstructs the 16-run evaluated-orbit
  registry and its closed dependency DAG from pinned bytes, freezes the 439-orbit
  coverage root and 64-pair outcome-blind selector, and publicly rebuilds every
  exact/depth trace, aggregate, assessment, inspection row, and evidence seal.
- Two independent final reviews found no remaining P0–P3 defect after adversarial
  tests for outcome leakage, self-resigned roots, dependency forgery, schedule
  routing, byte races, ancestry, failure sidecars, and internal symlink redirects.
- All 261 dependency-free tests pass at the Plan 0009 pre-outcome implementation
  checkpoint.
- Plan 0009's outcome-free manifest reproduced 724 fresh and 563 paired-valid
  orbits, then froze 64 disjoint pairs across 16 strata. Exact and depth-5 stages
  completed every fixed slot without censoring, horizon, or direction mismatch.
- Fresh capture treatment was A/B = 3/29 at both v3 and v4. The registered boundary
  failed with zero B-win increase and one improving cell; all 64 strong treatment
  profiles were 30–0 role-dominant and candidate-shaped interaction was empty.
- Public reconstruction and an independent post-outcome audit matched the Plan
  0009 manifests, ancestry, traces, aggregates, assessments, and 44-case inspection
  without a P0–P3 defect.
- Plan 0010's outcome-free implementation now proves the one-to-two-runner
  transformation and source-witnessed inverse across all 8,128 relevant D4
  transforms, plus natural termination by treatment ply 13/14 and a 69,120-state
  structural bound.
- Its closed 21-artifact history projects 2,115 definition occurrences to 1,044
  exact definitions and 1,040 D4 identities. The evaluated exclusion projection
  is the disjoint 439+64=503 orbit union, and selected source and treatment
  definitions have zero full-history collision.
- The outcome-free census reproduced 1,016 relevant, 907 unused, 738 paired-valid,
  and 477 treatment-unique pairs; all 16 strata contain 19–36 eligible pairs and
  the deterministic selector fixes four per stratum. Bundle, projection,
  representative, pool, and selection roots are frozen in the active plan.
- Two independent pre-outcome reviews found no P0–P3 defect in this pure slice,
  including the static-corpus adapter and all six manifest-root failure guards.
  All 277 dependency-free tests pass; no Plan-0010 manifest has been written and
  the Plan-0010 builders neither called solver/play nor computed an outcome.
- Plan 0010 now also has a pure fixed source-then-treatment exact evaluator. It
  reconstructs source-witnessed runner lineages from retained actions, validates
  every completed or censored slot against the side-specific state proof, rejects
  `PLY_LIMIT`, seals the complete source phase before treatment, and rebuilds all
  exact aggregates, response assessments, and inspection rows from raw slots.
- Independent exact-slice review found no remaining P0–P3 defect after adversarial
  checks for pair-fingerprint forgery, manifest mutation/replacement, lineage
  tampering, impossible bound-equality censors, schedule changes, and derived-field
  tampering. All 288 dependency-free tests pass. This checkpoint still contains
  no production manifest or outcome.
- Plan 0010's pure depth-5 evaluator now fixes all source profiles before all
  treatment profiles, gives each definition one fresh agent and one cumulative
  5,000,000-node budget over seeds 0–29, retains censored prefixes without routing
  later definitions, and reconstructs every game, lineage, node ledger, profile,
  assessment, breakdown, seal, and inspection row from raw evidence.
- Strong engagement requires one completed treatment game that both moves the
  added lineage and exposes a both-lineage B decision. Exact PVs, cross-game ORs,
  and censored prefixes cannot substitute. Treatment shape failures remove the
  frontier, source shape failures do not, and dominance removes only candidate
  shape. Independent depth review found no remaining P0–P2 defect.
- Three noninjectable Plan-0010 one-shot edges now bind the reviewed closed bundle,
  its exact ledger bytes, the Plan-0009 outcome-free five-file chain, the active
  plan blob, and a 31-file executable closure into strict clean-commit provenance.
  Separate manifest, exact, and depth reservations, attempts, failures, ancestry,
  byte seals, locks, symlink guards, and final return-time reseals are independently
  reviewed. All 330 dependency-free tests pass; no production artifact or outcome
  has been created.
- The reviewed depth evaluator is committed at `758a6b9e34435c7ad6b004f04e7e9b38bf81e909`
  and the strict one-shot evidence chain at
  `50372757ce1a9c60d1a650000bcc2a845f6dd1b3`. Final integrated pre-outcome review
  found no remaining P0–P3 defect across membership, evaluation, provenance, and
  execution boundaries.
- Plan 0010's outcome-free manifest was frozen once at `eb0e032`: 64 fresh pairs
  across all 16 strata, the declared 1,016/907/738/477 census, and no historical
  collision. Both outcome-use provenance flags are false.
- Two-runner exact solving completed 128/128 at `3450947` without censor or
  `PLY_LIMIT`. Source A/B was 35/29 and treatment was 34/30; only one value changed.
  Containment response was zero, while strict exact engagement occurred in 31
  cases across 12 strata.
- Two-runner depth 5 completed 128/128 profiles and 3,840 games at `d430e83`
  without node censor or direction mismatch. Fifty-four treatments had same-game
  two-lineage engagement and 46 entered the strong frontier across every stratum,
  but all 46 were 30–0 role-dominant and candidate shape was empty.
- Public post-commit reconstruction and two independent audits matched the full
  Plan-0010 manifest/exact/depth traces, lineages, node ledgers, assessments,
  inspections, ancestry, and byte seals without a P0–P3 defect. The formal result
  is `NOT_SUPPORTED_TWO_RUNNER_SETUP` with branch `FAMILY_REASSESSMENT`.
- Plan 0011's pure pre-outcome slice now exhaustively enumerates the native
  4×4/max-8 vocabulary and reproduces 16,320 raw definitions, 2,040 D4 orbits,
  1,592 gate-valid orbits, 22 historical intersections, 14 eligible exclusions,
  and 1,578 fresh gate-valid orbits across all 32 declared strata.
- Its closed-history projection replays the reviewed 21-artifact ledger graph and
  the separately authenticated outcome-free Plan-0010 manifest from exact bytes:
  2,243 occurrences, 1,172 exact definitions, 1,168 D4 identities, and the
  declared 1/5/9 exclusion carriers with one overlap. Unknown source paths,
  changed bytes, identity drift, inner self-signing, and outcome-bearing manifest
  metadata fail closed.
- The one-per-stratum selector fixes 32 canonical fresh definitions with eligible
  pool root `963ab02e…44e99` and selection fingerprint `bcf9992c…ce834`.
  Public proof reconstructs the 62,096/40,272 A/B-first state bounds and the
  1,637,888 fixed-corpus sum below the unchanged 100,000 per-case cap.
- Independent algorithm and provenance reviews found and closed strict-type and
  forged-object parser bypasses, then reported no remaining P0–P3 defect. All 343
  dependency-free tests pass. No production Plan-0011 manifest, exact label,
  sampled profile, reservation, lock, or outcome artifact exists.
- Plan 0011 now also has a pure fixed exact evaluator. It binds every one of 32
  manifest-order slots to the reconstructed case and state proof, replays complete
  principal variations, rejects a state censor as a proof contradiction, and
  rebuilds every label, aggregate, definition-only breakdown, coverage decision,
  evidence digest, and inspection row from raw evidence.
- `DRAW/PLY_LIMIT` is retained as `HORIZON_DRAW`, while a ply-8 `GOAL` remains
  decisive. Sufficient label coverage only authorizes the future depth-5 stage;
  insufficient coverage blocks it and retains the predeclared family-retirement
  branch. No selected definition has been solved and no production exact label or
  artifact exists.
- Three independent exact-slice reviews found and closed timing-overflow,
  impossible search-accounting, and injected-callback mutation defects, then
  reported no remaining P0–P3 defect. All 353 dependency-free tests pass. Minimax
  origin remains intentionally assigned to the fixed executor and future one-shot
  executable/byte provenance rather than inferred from PV replay alone.
- Plan 0011 now also has a pure fixed depth-5 evaluator over all 32 manifest-order
  cases and seeds 0–29. It requires trusted sufficient exact coverage before the
  first agent, exposes no outcome labels through the agent schedule, creates one
  fresh reset cumulative-budget agent per definition, and retains complete game,
  action, terminal, and node evidence or a strict censored prefix.
- Public reconstruction checks every trace and root-action node lower bound,
  exact-direction confusion, exact-draw decisive errors, all-case and
  definition-only breakdowns, timing-free evidence seals, and complete inspection.
  Node censoring has assessment priority; otherwise support requires at least 90%
  decisive-direction accuracy and zero decisive call on an exact horizon draw.
- Three independent depth-slice reviews found and closed standalone coverage,
  strict-boolean, incomplete-prefix node, runtime agent-drift, and hostile-input
  mutation gaps, then reported no remaining P0–P3 defect. The fixed public
  Minimax path has not run. All 367 dependency-free tests pass, and no production
  manifest, exact label, sampled profile, reservation, lock, or outcome artifact
  exists.
- Plan 0011 is closed as `SUPERSEDED_PRE_OUTCOME`, not as an evaluator result. Its
  census/selector/state-proof, exact-evidence, and depth-evidence slices remain
  preserved at `96fa5b7` with their roots, schedules, thresholds, seeds, caps, and
  367-test checkpoint. No production manifest, label, profile, run, reservation,
  attempt, failure, lock, or outcome exists, and its one-shot edge was never
  implemented or invoked.
- Plan 0012's pure family-identity scaffold separates a name-free mechanical
  signature from stable registry labels and ordered registry identity. It uses
  closed enums only, reparses at public hash/registry boundaries, and can
  represent all six provisional non-`PLACE`/`MOVE` cells without freezing their
  final membership or search domains.
- Its canonical signature and registry golden vectors, exact terminal-order
  labels, restricted v1 vocabulary, caller detachment, and construction-snapshot
  mutation guards passed independent adversarial review with no remaining P0–P3
  finding. All 381 dependency-free tests pass.
- Schema v4 now freezes first-player/actor-priority cross-role goal semantics and
  implements PUSH as its first atomic action. PUSH supports ordinary relocation
  or one adjacent-opponent displacement into one empty in-bounds landing cell;
  actions retain only canonical from/to evidence and replay derives the effect.
  Its local legal-action predicate matches an independent reference over all
  59,049 one-pusher 3×3 occupancy assignments.
- The v4 object graph is strict under parsing and direct construction, while
  canonical JSON, hashes, and prose revalidate exact typed parser normal form.
  D4 transforms PUSH vectors, and simplicity charges its distinct primitive,
  per-role conditional form, and extra transition substep. Independent DSL review
  found and closed string-enum, mutable-container, hidden-field, and forged-object
  paths, then reported no remaining P0–P3 defect. All 409 dependency-free tests
  pass; no compiler, telemetry, Plan-0012 solver/agent run, gameplay experiment,
  or outcome artifact was created.
- Schema v4 now also implements SWAP as ordinary adjacent relocation into an
  empty cell or exchange with one adjacent opponent. The actor and target retain
  owner and piece kind; friendly, ranged, multi-piece, and undeclared-vector swaps
  are illegal, and replay still derives the complete effect from `kind/from/to`.
  Schema-v4 admission now requires at least one `PUSH` or `SWAP` role.
- The fixed SWAP DSL and D4 hashes are
  `c8dba41bb2e97e5fcc5dd119a415da2ee74b68e196f38c70d2aada292de73ba2` and
  `aae4af10c105a9e498277a0df32024f3bdbd4baf91ef95368ba2d9985030c4b0`.
  SWAP legality matches an independent predicate over all 59,049 local 3×3
  occupancies; schema-v1/v2/v3 comparison found zero differences over 3,600
  states and 7,804 transitions, PUSH comparison found zero over 1,200 states and
  835 transitions, and the SWAP D4 comparison found zero over 2,368 cases. The
  focused regression suite passes 99/99, and the complete dependency-free suite
  passes 425/425 tests in 332.954 seconds. No Plan-0012 solver, agent, gameplay
  experiment, or outcome was run or created in this slice.
- Schema v4 now also implements HOP as ordinary adjacent relocation or a jump
  over exactly one adjacent occupied piece to the empty in-bounds cell one more
  step along the same declared vector. Friendly and opposing pieces of any kind
  can be jumped and remain completely unchanged. Empty intermediates, occupied or
  off-board landings, undeclared directions, longer moves, and chains are illegal.
  A special action records only `kind/from/to`, with `to` fixed to the final
  landing; replay derives the intermediate cell. Schema-v4 admission now requires
  at least one `PUSH`, `SWAP`, or `HOP` role.
- The fixed HOP DSL and D4 hashes are
  `4ad5598c0b5a15cb24fa9b79747634c7a8ea87fa909f40cb23ad2181528015ce` and
  `94d8d41c58a35f6882846ebefe0fb7a19ab2787238819e83e61773fc54266b95`.
  HOP legality matches an independent predicate over all 59,049 local 3×3
  occupancies, including exactly 87,480 ordinary actions and 23,328 special hops
  split evenly between friendly and opposing intermediates. D4 comparison found
  zero differences across 472,392 legality cases and 886,464 transitions; the
  SWAP-checkpoint comparison found zero differences across 6,000 prior-schema,
  PUSH, and SWAP states and 11,822 transitions. Independent reviews closed one P3
  ordered-pair test-coverage gap and reported no remaining P0–P3 finding. The
  focused regression suite passes 117/117, and the complete dependency-free suite
  passes 443/443 tests in 333.079 seconds. No Plan-0012 solver, agent, gameplay
  experiment, or outcome was run or created in this slice.
- Schema v4 now also implements CONVERT as a vector-bearing action that does not
  move its actor. One matching owned actor stays in place and changes exactly one
  adjacent opponent target of any kind into an actor-owned piece of the action's
  declared kind at the same target cell. Empty, friendly, ranged, undeclared,
  and multi-target conversion is illegal; an opponent target already of the
  declared kind remains legal because ownership changes. Recorded evidence is
  still exactly `kind/from/to`. Schema-v4 admission now requires at least one
  `PUSH`, `SWAP`, `HOP`, or `CONVERT` role.
- The fixed CONVERT DSL and D4 hashes are
  `9233c363533156baa3553832d277810658256b65ddac28490af19b15e5478429` and
  `64f7f012a33713f90276d2cd2ecfdc5bef8c5966295632c575a64c8dcbbc1f0f`.
  CONVERT legality matches an independent predicate over all 59,049 local 3×3
  occupancies, producing exactly 87,480 legal conversions with the frozen
  0-through-8 action-count histogram. D4 comparison found zero differences over
  472,392 legality cases and 699,840 transitions; the HOP-checkpoint comparison
  found zero differences over 7,200 states and 19,301 legal transitions. Three
  independent reviews reported no P0–P3 finding. The focused regression suite
  passes 138/138, and the complete dependency-free suite passes 464/464 tests in
  333.681 seconds. No Plan-0012 solver, agent, gameplay experiment, or outcome
  was run or created in this slice.
- Pre-implementation review rejected `CAPTURE_STEP` as a semantic alias rather
  than adding another executable DSL `ActionKind`. Its proposed optional empty
  step or adjacent capture, single-target removal, final actor position, action
  record, D4 action, and v4 terminal behavior are already supplied by
  `MOVE_CAPTURE`, which schema v4 accepts. A second spelling would manufacture
  family, DSL, D4, asymmetry, simplicity, and census differences without changing
  the game graph.
- The two provisional removal cells now reuse `MOVE_CAPTURE`; they remain new
  complete family topologies because capture/eliminate faces hop/reach, and
  convert/eliminate faces capture/eliminate. The family-signature-v1
  `CAPTURE_STEP` proposal token is retained only for identity compatibility and
  is excluded from the current provisional cells and future executable compiler.
  The explicit `NONRETIRED_ACTION_PRIMITIVES_V1` allow-list and
  `validate_nonretired_family_signature`/`registry` boundaries preserve legacy
  parse/hash reconstruction while making future active emission fail closed.
  Mandatory capture is reserved as a genuinely different possible future
  mechanic. The all-occupancy oracle checked 59,049 states, 174,960 actions, and
  the exact 0-through-8 histogram; D4 comparison found zero differences over
  472,392 legality cases and 1,399,680 transitions. Three independent reviews
  found no remaining P0–P3 issue. The complete dependency-free suite passes
  472/472 tests in 350.375 seconds. No outcome, solver, agent, or gameplay
  experiment informed this correction.
- Schema v4 now implements `ELIMINATE(piece)` as a non-spatial state goal that is
  true exactly when the opponent owns no piece of the declared target kind. It
  adds no edge, counter, owner, or history field; initial zero targets are
  deliberately terminal and must be excluded by later feasibility. Schemas
  v1–v3 reject it through parser and direct-construction boundaries. V4 admits a
  definition only when it contains a PUSH/SWAP/HOP/CONVERT action or an
  ELIMINATE goal; all 441 ordered action/goal combinations produce the fixed
  405-admit/36-reject partition.
- The fixed asymmetric ELIMINATE fixture has exact DSL hash
  `a3a1761e1a04e62f95d69e0190d7368936842fdaf40a123f02a0f5696ec8f0a2`
  and D4 hash
  `3a104109d52a633fb2b698fdfffa7f7189f373ad357d5796e7accba923013030`.
  Its goal predicate matches an independent oracle across all 19,683 ternary
  3×3 occupancies and both roles: neither 18,660, A-only 511, B-only 511, both 1.
  The eight D4 maps produce zero differences over 314,928 predicate checks.
- Static reachability permits an unsatisfied ELIMINATE goal only for
  MOVE_CAPTURE/CONVERT with a matching initial owned action actor; the action and
  target piece kinds need not agree. Simplicity adds only a shared goal primitive
  and learned concept. Goal-directed-v1 and minimax-v1 fail closed before action,
  RNG, or node work because no elimination heuristic is calibrated. Three
  independent reviews closed one P3 implicit-CONNECT analysis branch and found
  no remaining P0–P3 issue. The complete dependency-free suite passes 495/495
  tests in 340.804 seconds. No Plan-0012 solver/agent run, gameplay experiment,
  or outcome was created in this slice.
- Replay telemetry v1 now reconstructs descriptive evidence from an authoritative
  schema-v4 definition and a complete retained trace. It freezes the interpreted
  action, goal, terminal, effect-mode, actor-relative outcome, and direct-effect
  vocabularies; uses exact one-ply counterfactuals; separates successor-position,
  state-effect, immediate-outcome, and next-player legal-set variation; and
  retains role branching/forcedness, chosen effects, repeated configurations,
  terminal evidence, bounded-work counters, and a canonical evidence digest.
  Strict action/stored-data validation and fixed replay, enumeration, and JSON
  limits fail closed. It defines no scalar agency score, policy, sampling rule,
  solver, or universal quality threshold.
- The frozen seven-fixture synthetic telemetry benchmark covers initial
  immobility, immediate reconvergence, same-count/different-set next-player
  actions, actor-relative win/draw/loss alternatives, one-sided and both-role
  direct effects, repetition, capture, conversion, elimination, and opponent
  dependency without direct effect. Definition hashes, trace digests, evidence
  digests, all-fixture D4 projections, the SWAP-cycle role-swap relation, and root
  `6789f97e7f0948df9fa66f75933c061a08e029ce49e9d9da33ae57a7b4a59a33`
  are fixed. This is a calculation benchmark, not a gameplay experiment or an
  outcome-based family comparison.
- Independent semantic and hostile-boundary reviews closed CONVERT
  classification, repeated-period, zero-action, typed/stored evidence,
  validation-ordering, and bounded-JSON defects, then found no remaining P0–P3
  issue. The focused telemetry suite passes 39/39 tests, and the complete
  dependency-free suite passes 534/534 tests in 337.888 seconds.
- The provisional feasibility domain is now an exact, deterministic 214,368-input
  construction: 2,436 balanced disjoint 3×3 setups, 11 family/frame occurrences,
  four ordered independent A/B vector-profile pairs, and two first-player
  choices. Its descriptor hash is
  `3456b5873dadfd177639b4c0d062bad6717d328a7ebee88a71f4294f9a5b0bc5`,
  and its unique ordered input root is
  `e3011f74256fe39881eda3636533ec4352f7b4d2c4386e12d775ccc031b6d2e8`.
- The pure schema-v4 compiler emits all six provisional family signatures and
  all nine named D4 goal-frame representatives, with 11 family/frame
  occurrences. A reconstructable witness binds domain, case, family, exact
  definition, name-free D4 identity, and an action-specific structural
  state/work proof. Fixed-count, convert/push, capture/hop, and
  convert/capture state bounds are respectively 14,364/31,920,
  26,334/67,032, 19,836/67,032, and 40,603/181,051 for two/three pieces per
  role; the maximum raw action-candidate bound is 48.
- Independent semantic and hostile-boundary reviews found no implementation
  P0–P2 issue. Their two P3 test-coverage findings were closed by frozen domain,
  definition, D4, witness, and ordered-enumeration goldens, all-input hash
  uniqueness, and an explicit B-first 3+3 compiler case. The focused feasibility
  suite passes 19/19. This slice imports no engine, agent, solver, play, replay,
  analysis, or outcome computation and creates no registry, search envelope,
  census result, gameplay, or outcome. The complete dependency-free suite passes
  553/553 tests in 347.078 seconds.
- The outcome-free feasibility census reconstructs all 214,368 inputs and
  102,336 D4 orbits. Exactly 71,384 exact inputs and 24,208 orbits pass no-initial-
  goal, action-aware connection-material, and both-role initial-mobility gates.
  Every transformed initial descriptor is invariant over 1,714,944 checks, and
  391 fixed cases spanning all 176 strata match the authoritative public
  compiler/D4 path byte for byte.
- All six family cells retain eligible support. There are 144 nonempty supported
  strata; the only unsupported 32 are the 2+2 fixed-count connection strata in
  `swap-hop-network-v1` and `push-swap-networks-v1`. Supported-stratum minima are
  12, 194, 138, 164, 89, and 402 eligible D4 orbits respectively for push-hop,
  swap-hop, convert-push, capture-hop, convert-capture, and push-swap.
- The compact census binds complete reason combinations, exact and D4
  distributions, case-to-orbit mapping, and orbit multiplicity/representative
  witnesses. Its evidence digest is
  `ade64ebaaf9892848e03c2312786a6ef3825f3c82f4311e16a9c810711d035d2`;
  ordered case-to-D4 and sorted eligible-D4 roots are `e016d464…100a6` and
  `6b96cf4e…d0108`. Independent implementation and output reviews report no
  remaining P0–P3 finding. No action was applied and no gameplay outcome exists.
- The complete dependency-free regression suite passes 571/571 in 651.869
  seconds at the Plan-0012 feasibility closeout.
- Plan 0013's pure selector reconstructs the complete density split rather than
  silently dropping 2+2: 33,264 raw/7,740 eligible exact setup pairs and 3,250
  eligible paired D4 identities are explicitly deferred, including 16 derived
  unsupported paired strata. The active 3+3 core contains 73,920 raw/27,952
  eligible exact setup pairs and 8,854 paired D4 identities across 44 paired
  strata.
- The cutoff-fixed history adapter authenticates all 66 experiment JSON blobs at
  commit `6910b6c`, retaining 2,243 definition occurrences, 1,172 unique exact
  definitions, 1,168 unique D4 identities, and one explicit malformed value.
  Its identity-only projection and the separate seven-fixture synthetic
  projection expose no result, winner, score, trace, agent, or outcome field to
  the selector.
- Historical collision with the active universe is zero. The frozen partition
  contains 144 development pairs/288 definitions, a disjoint 144-pair/288-
  definition candidate block, and 8,566 untouched pairs/17,132 definitions,
  with equal 24-pair/48-definition nominal exposure per family.
- Exact A-first/B-first setup correspondence, ordered member D4 identities, all
  eight representative orientation hashes, pair multiplicity, common proofs,
  and pair/definition counts are bound into the paired-universe witnesses. The
  search-envelope, universe, history, collision, and selection roots are
  `eb875a7d…f6ead5`, `1167ac8d…cab15`, `6e99ceca…ab08d`,
  `b6b7f14e…fab354`, and `de65bf68…67830`; canonical selection SHA-256 is
  `b681c32b…68b5`.
- The production selector accepts only the two reviewed frozen projection roots.
  Cutoff commit blobs are compared byte-for-byte, generic collision APIs are not
  exported, and self-resigned projection, pairing, orientation, census, and
  selection tampering fail closed. Independent final review found no P0–P2
  defect. The complete suite passes 604/604 in 975.642 seconds at selector commit
  `5bce4c0`; no selected definition was played or solved.
- Plan 0013's separate `terminal_only_minimax-v1` is family-neutral by
  construction: A-relative terminal utility is `+1/0/-1`, every nonterminal
  cutoff is zero, A maximizes, B minimizes, canonical ties alone consume seeded
  RNG, and cache-miss successors consume a cumulative per-role-slot node budget.
  Exact schema-v4/state/action/identity boundaries, depth `1..64`, reset, cache,
  and failure-audit behavior fail closed; a valid schema-v4 PLACE-created piece
  is explicitly accepted on the following turn.
- The five-fixture conformance benchmark reuses four Plan-0012 identities and
  adds one noncolliding SWAP/CONNECT fixture. It freezes 40 D4 exact/search slots,
  320 complete/replayed random games, 320 complete/replayed terminal-only games,
  and 80 cumulative role slots. It dynamically distinguishes root and recursive
  B minimization, terminal priority, all five active action kinds, all three goal
  kinds, A/B/draw, cutoff, cache, tie, and budget behavior.
- Fixture separation from the 288 development and 288 candidate definitions is
  proved before any solve/search/play call. Production definition access and
  production outcomes are both zero. The fixture/ladder/random/terminal/
  evidence/separation/benchmark roots are `9a45e26f…99f565`,
  `62255ad5…65e62`, `50a0ef2d…77576`, `38dce33d…480026`,
  `5cd0dd2a…f8d075`, `a82e7640…51eba`, and `340e928d…15da0`;
  canonical SHA-256 is `5097d4d5…a3ffc`.
- Two independent final reviews found no remaining P0–P2 defect after strict
  provenance, min/max, lifecycle, mutation, resource-bound, and no-leakage
  probes. The complete dependency-free suite passes 635/635 in 979.572 seconds
  at evaluator commit `d34c383`.
- Plan 0013's protocol checkpoint fixes the full stage order before any selected
  outcome: manifest, 288 exact solves with 2,304 D4 principal-variation replays,
  18,432 random games, 18,432 terminal-only depth-1 games, telemetry for all
  36,864 sampled traces, then assessment and inspection. It also fixes 18,432
  matched-start blocks, a 19,722,000-state total exact ceiling, a 592,781,760
  state-action-candidate ceiling, and a 15,925,248-node sampled-search ceiling.
- Exact labels distinguish A-role, B-role, first-player, second-player, and
  unresolved horizon effects. The weak sampled gate requires all 128 games per
  pair and strength, at least 64 decisive games, and the exact integer fairness
  inequality `20 × min(A wins, B wins) >= 7 × decisive games` at both random and
  depth 1. A family signal requires at least two frontier pairs in at least two
  strata and remains descriptive rather than a fairness or enjoyment claim.
- The protocol's typed immutable evidence chain covers reservation, attempt,
  failure, completion, blocked descendants, and hard-crash `ORPHANED` recovery.
  Outcome stages rebuild fixed schedules through protocol-only iterators and
  never receive the selection or candidate-definition block. Schedule roots for
  exact/orientation/profile/role/game/matched/telemetry are `de198bc1…a1a7`,
  `931e48a2…84e`, `c2624a0d…26a`, `4d3e945b…2b48`, `da59e67a…0f18`,
  `112babd7…c3ea`, and `be0a229a…2867`. The original V1 execution-envelope
  root `d933c779…7fd3` at `28e6a03` was superseded pre-outcome after implementation
  review found bootstrap and recovery-authentication gaps. No scientific
  schedule, coordinate, cap, label, threshold, or inspection rule changed.
- The V2 protocol root is `8126c33f…f04c`; its 2,220,598 canonical bytes have
  SHA-256 `b732f174…9229`. A candidate-neutral bootstrap now authenticates the
  protocol, immutable plan, and all six recursive production closures before
  manifest reservation. Every later parent chain and completion body is
  reauthenticated against that bootstrap, and immutable per-slot journals
  rebuild exact 288+2,304, random 18,432, depth-1 18,432, and telemetry 36,864
  status ledgers without trusting caller-supplied schedules.
- Six independent entrypoints now implement manifest, exact, random, depth-1,
  telemetry, and assessment `run`/`recover` boundaries. `FAILED`, `BLOCKED`,
  `ORPHANED`, and `COMPLETED` are exclusive; contradiction sidecars, residual
  evidence, alternate ancestors, closure drift, and crash-publication windows
  fail closed. A failed manifest can produce only sealed blocked descendants and
  a rooted zero-outcome meta assessment.
- Independent architecture and provenance reviews found no remaining P0–P2
  defect after candidate-boundary, cache-race, alternate-parent, completion,
  status, sidecar, and recovery probes. At commit `0697800`, focused runs passed
  25/25 protocol tests in 337.099 seconds and 109/109 stage/evidence tests in
  21.400 seconds; the complete dependency-free suite passed 769/769 in
  1,085.011 seconds. No production evidence directory, manifest reservation,
  selected-game solve, sampled game, telemetry record, assessment, or candidate-
  block allocation was created.
- The outcome-free manifest stage ran once from clean HEAD `5e6a865` and sealed
  `COMPLETED`. Bootstrap, manifest, and terminal roots are `96c7e239…ea22`,
  `ffbb2256…f7c3`, and `5ee92d2b…e46`; the canonical detached-manifest blob is
  2,681,806 bytes with SHA-256 `d3eb19a3…ee89`. It contains 144 pairs, 288
  definitions, and 2,304 orientations, evenly split as 24/48/384 in every
  family. Candidate-block overlap and export are zero, and gameplay outcome
  count is zero.
- Public recovery returned `VERIFIED_NO_OP`. Three independent read-only audits
  reconstructed the fixed selection, all manifest definitions and D4 transforms,
  the bootstrap and stage BodyRefs, all six 59-file closure records, lifecycle
  identities, and fixed paths with no P0–P3 finding. The focused evidence/data/
  manifest suite passes 72/72. At the manifest terminal checkpoint, no
  downstream stage directory yet existed.
- All six V2 stages now have terminal seals, so the temporary fixed-HEAD
  execution condition has ended. Every terminal binds source HEAD `5e6a865` and
  tree `17468fd2…fad`. This transition archives the complete immutable store and
  documentation; regenerable `.locks/*.lock` files are excluded as operational
  state.
- The exact stage ran once and sealed `COMPLETED` at terminal identity
  `6c575e50…a6c45`. Exact status is 288/288 `COMPLETE`; PV status is
  2,304/2,304 `VALID`; invalid and proof-contradiction counts are zero. The
  solver used 2,538,786 states, 12.87% of the frozen aggregate ceiling, with
  maximum 29,610 in one definition.
- Raw exact outcomes are A/B/draw = 98/179/11; terminal reasons are GOAL 246,
  `NO_LEGAL_ACTION` 31, and `PLY_LIMIT` 11. Pair labels are 51 first-player-
  dominant, 23 A-role-dominant, 62 B-role-dominant, zero second-player-dominant,
  and eight horizon-affected. The exact gate retains 51/144 pairs across 31/44
  strata. At that exact-stage boundary, later weak-balance gates were still
  unobserved.
- Public exact recovery returned `VERIFIED_NO_OP`. Three independent read-only
  audits replayed all exact/D4 PVs and rebuilt the 5,184 journal files, 2,592-
  record catalog, full ledger, summary, parent chain, and source closures with no
  P0–P3 finding. The focused exact/evidence suite passes 49/49.
- The random stage ran once and sealed `COMPLETED` at terminal identity
  `bafc4a52…e9b6`. All 18,432 games are `COMPLETE`; incomplete, invalid, and
  proof-contradiction counts are zero. Raw A/B/draw is 7,142/9,470/1,820,
  terminal reasons are GOAL 15,700, `NO_LEGAL_ACTION` 912, and `PLY_LIMIT`
  1,820, and the games contain 105,440 total plies.
- The frozen random weak-balance gate retains 26/144 pairs across 18 strata:
  push-hop-race 9, convert-push-front 9, and push-swap-networks 8. Its
  intersection with the exact entrance gate retains only 16 pairs across 12
  strata: 9 push-hop, 5 convert-push, and 2 push-swap. All 16 are exact
  first-player-dominant, so they remain two-game role-swap routing signals rather
  than fair single-definition or enjoyment claims.
- Public random recovery returned `VERIFIED_NO_OP`. Three independent read-only
  audits reconstructed all 36,864 start/result journals, replayed every one of
  the 105,440 random choices and actions from its seed, and rebuilt the schedule,
  ledgers, catalog, parent chain, and closure with no P0–P3 finding. The focused
  random/evidence suite passes 47/47.
- The terminal-only depth-1 stage ran once and sealed `COMPLETED` at terminal
  identity `f5d1e937…be226`. All 18,432 games are `COMPLETE`; no node censor,
  invalidity, or proof contradiction occurred. Raw A/B/draw is
  6,380/11,806/246, terminal reasons are GOAL 16,802, `NO_LEGAL_ACTION` 1,384,
  and `PLY_LIMIT` 246, with 56,788 total plies and 279,242 expanded nodes.
- Depth-1 weak balance retains 48/144 pairs. Its intersection with the 51 exact
  and 26 random entrances is 14 pairs across 10 strata: push-hop-race 9/6,
  convert-push-front 3/3, and push-swap-networks 2/1. Under the frozen family
  count rule, only push-hop and convert-push reach the required two pairs in two
  distinct strata. These are raw parent-evidence diagnostics because the final
  assessment did not produce formal family statuses.
- All 14 pair-frontier signals are exact first-player-dominant. In depth-1 each
  is A/B = 64/64 only because A wins every A-first game and B every B-first
  game. This supports the matched two-game role-swap unit, not fairness of a
  single DSL definition. It is not evidence of strategic depth or enjoyment.
- Public depth-1 recovery returned `VERIFIED_NO_OP`. Three independent read-only
  audits replayed all 18,432 games and 56,788 decisions, recomputed action
  values and 279,242 node expansions, and rebuilt cumulative role-slot ledgers,
  catalogs, schedules, parent seals, and closures with no P0–P3 finding. The
  focused depth-1/evidence suite passes 47/47.
- The replay-telemetry stage ran once and sealed `COMPLETED` at terminal
  identity `2869fc55…f670`. All 36,864 sampled traces are `VALIDATED`; missing,
  not-admissible, invalid, and proof-contradiction counts are zero. The trace
  corpus contains 162,228 plies, exactly random 105,440 plus depth-1 56,788.
- Descriptively, telemetry records A/B decisions 79,634/82,594, forced decisions
  3,957/1,104, and opponent-dependent actions 38,894/28,330. Both roles execute
  an opponent-dependent action in 10,850 games; 1,261 games repeat a full
  configuration. These are route- and agent-dependent diagnostics, not formal
  gates, agency scores, fairness evidence, or enjoyment evidence.
- Public telemetry recovery returned `VERIFIED_NO_OP`. Independent science and
  chain audits reconstructed every parent/sample/telemetry root and all 36,864
  telemetry records from the DSL traces with no P0–P3 finding; the filesystem
  audit independently checked the same complete catalog. The focused telemetry/
  evidence suite passes 53/53.
- The one-shot failure machinery worked fail-closed. The assessment reservation,
  sole attempt, exception body, and terminal seal are retained; no partial or
  completed report can be mistaken for having existed. Public recovery returns
  `VERIFIED_NO_OP`, and independent audits found no P0–P3 defect in the sealed
  failure chain or its five parent bindings.
- Root-cause audits isolated two assessment-side defects without modifying V2:
  the trusted caller passed a tuple to an exact-list telemetry-ledger boundary,
  and assessment misread the zero-legal-action observation recorded at
  `NO_LEGAL_ACTION` terminals as a decision. All and only 2,296 telemetry games
  exhibit the latter shape; under the authoritative telemetry semantics the
  remaining checks pass 36,864/36,864. The parent evidence is not corrupted.
- The final unchanged-code boundary suite passed all 769 dependency-free tests
  in 1,073.706 seconds. This confirms the documented integration-test gap: the
  old test suite passes despite the two production-join defects, so Plan 0014
  must add tests that traverse that real boundary.
- Plan-0014 slice 1 now separates the frozen assessment calculation/read core
  from the historical write-capable lifecycle facade. Only the exact-list
  producer and authoritative zero-legal-observation identities differ from the
  archived implementation; AST comparison found every other function unchanged.
- The V2 reader is truly no-create and descriptor-bound. It traverses every
  repository/store component with `openat` and `O_NOFOLLOW`, rejects ancestor
  symlinks, retains the authenticated inode across path replacement, creates no
  lock or contradiction directory, and rejects every publication path.
- The strengthened real-parent integration test passed in 541.990 seconds. It
  reconstructed all 36,864 telemetry records and all formal raw inputs while a
  152,716-entry filesystem inventory remained identical. The inner report root
  is `2a873b0b…5484d`; inspection is 98 pairs, 118 class groups, 104,458 bytes,
  SHA-256 `0fc52593…03c7e`. Independent slice-1 re-audit found no P0-P3 issue.
- The reconstructed inner calculation is `FORMAL_COMPLETE` with claim level
  `FAMILY_FRONTIER_SIGNAL_NOT_FAIR_GAME`: 14 pair-frontier signals are all exact
  first-player-dominant. The frozen count rule marks push-hop-race 9/6 and
  convert-push-front 3/3 positive, but this remains an unsealed post-failure
  calculation rather than fairness, strategic-quality, fun, or confirmation
  evidence.
- The Plan-0014 slice-1 implementation boundary passed all 799 dependency-free
  tests in 1,044.068 seconds. The only skip was the deliberately opt-in real-V2
  reconstruction, which had already passed separately in 541.990 seconds.
- Plan-0014's first complete implementation checkpoint `37d95eb…d58ee` is
  rejected before production. Three independent audits found that an allowed
  FIFO could block `O_RDONLY` before the regular-file `fstat`; its protocol root
  `5424b2d0…f8458` and SHA-256 `adb409ba…fa88` are retained only as superseded
  history. No Plan-0014 evidence root existed at that checkpoint.
- The separate reader-hardening checkpoint `bcec701…ed057`, tree
  `69553e07…4cb0`, adds nonblocking no-follow opens, exact bounded reads,
  descriptor-walked current-source paths, and nonmasking lock cleanup across
  the old reader, new attestation, new evidence store, and stage source edge.
  Its exact change surface is five production and four test files. FIFO,
  socket, device, symlink/hardlink, size, and path-swap regressions pass. This is
  operational/type hardening with zero new assessment corrections and zero
  scientific-rule changes; the two allowed repair defects remain unchanged.
- The hardened final report-only protocol has root `b412e0af…5d38d`, 16,880
  canonical bytes, and SHA-256 `0aeb2bf4…cfd1`. Its exact 13-path recursive
  production closure requires byte equality for three scientific repair/facade
  blobs, four non-protocol reader-hardening blobs, and six unchanged scientific
  dependencies. The final protocol module is self-bound by its production-
  closure record. Reflective imports and every solver, agent, gameplay,
  telemetry-generation, selector, and candidate-block execution dependency
  remain excluded.
- The real cross-store source binding matched independently at
  `f763bad5…e5157`. Archive and live views both contain 152,680 tracked files and
  601,787,372 bytes at inventory root `773941af…d612`; optional operational
  locks and the empty contradiction directory are identity-excluded but strictly
  checked when present.
- The full source-binding/report/outer-attestation test passed read-only in
  765.274 seconds and retained inner report root `2a873b0b…5484d`, canonical
  BodyRef SHA-256 `4acd5dda…a7c0`, and inspection SHA-256
  `0fc52593…03c7e`. This is still test-time reconstruction, not sealed evidence.
- The dedicated Plan-0014 evidence edge now has one noninjectable `run|recover`
  facade, immutable bootstrap/reservation/attempt/terminal artifacts, atomic
  pending reconciliation, completed-body precedence, no calculation resume,
  and descriptor-bound locks and publications. Independent review exposed and
  closed an operational-only clean-HEAD liveness bug and an inherited-directory
  durability window; real Git and child-then-parent fsync regressions now pass.
- Plan 0014's final source is `3fa9f6b…3f992`, tree `f491a361…6a50`, with exact
  13-path closure `975541c3…57d7`. The 874-test final suite and both real-data
  boundaries passed before three independent pre-run audits reported no P0–P3
  finding.
- The Plan-0014 production stage ran exactly once and sealed `COMPLETED` at
  terminal `9272adcd…b5ae`; public recovery returned `VERIFIED_NO_OP`. Its five
  immutable canonical artifacts total 334,180 bytes and contain no failure,
  orphan, contradiction, or pending artifact.
- The sealed inner report is `2a873b0b…5484d`, the inspection root is
  `279b0459…86b6`, and the outer attestation is `9ca2d719…5290`. Three independent
  post-run audits reported P0=P1=P2=P3=0; the semantic audit rebuilt the complete
  result from old evidence in 750.529 seconds.
- Independent pre-plan derivations agree that the Plan-0015 grammar contains 16
  coherent role atoms, 109 fresh role-neutral semantic classes, 1,518 fresh
  asymmetric profiled skeletons, and 5,111,055 factorized D4 setup carriers in
  the 15-resource-pair syntactic master census. These are preregistered arithmetic
  targets, not yet implementation evidence or game-quality claims.
- Plan-0015 slice 1 now lives in the standalone capability-minimal
  `parity_forge_universe` package, leaving the frozen, eager
  `parity_forge/__init__.py` byte-identical. Its production closure imports only
  the standard library and has no engine, transition, terminal, outcome, solver,
  agent, telemetry, selection, or candidate capability.
- The implementation exactly closes 16 role atoms, 256 ordered signatures, 36
  legacy-only rejections, 220 admissions, 115 role-neutral classes, 109 fresh
  classes, 49 raw/14 ordered/10 role-neutral goal frames, and three invariant
  movement profiles. It closes 3,300 ordered skeletons, 66 self-isomorphic
  exclusions, the complete 198 ordered/99 neutral old-six region, and 1,518
  fresh canonical skeletons with the preregistered stabilizer histograms.
- All 3,300 canonicalizations retain a byte-minimum D4-plus-role-swap witness and
  exact inverse. Eighteen ordered roots, their first/last endpoints, and the
  final descriptor seal reproduce the fixed root
  `05826dc2da02890f3f562ee2d6febda523c58837affb757b5dff6d62c074e6ab`.
- Enum names, values, registries, spatial transforms, profile definitions,
  old-six membership, hash domains, expected arithmetic, and the descriptor
  trust anchor now fail closed or derive from code literals. Independent
  adversarial review closed alias, consistent fake-member, mutable-authority,
  duplicate-shadow, and forged-endpoint attacks with no remaining P0-P3 finding.
- The final 47-test typed-universe boundary passed in 1,119.141 seconds. Two
  independent final code/test reviews and a separate symbolic oracle report
  P0=P1=P2=P3=0 on package/source/test blobs `6a7aba9e…5bd3`,
  `55cc8f7c…71f7`, and `49376caf…c91b`; the symbolic oracle independently
  reproduced all 18 roots, all 18 endpoint pairs, and the 3,300-witness root.
- The complete dependency-free boundary passes 921/921 tests in 2,188.935
  seconds, with only the two intended opt-in real-evidence reconstructions
  skipped. The frozen legacy package initializer remains blob
  `dd354f2588f985a66dd0014de5805b7ca1bc7781`.
- Three independent slice-2 design derivations reproduce the exact 6,798 setup
  rows, count-pair supplies, 10,319,364/20,638,728 master products, 5,111,055
  factorized carriers, and 11,652 fresh transport skeletons. They also confirm
  that importing the authoritative DSL in production necessarily loads the
  frozen engine; D-045 therefore fixes a pure canonical-wire compiler with
  authoritative parsing and transform comparison confined to tests.
- Plan-0015 slice 2 now compiles every strict skeleton/setup/first-player member
  to canonical schema-v4 wire JSON and reconstructs its complete typed source.
  Factorwise exhaustive tests cover all 1,518 fresh skeletons, all 6,798 setups,
  both first players, all D4 transforms, owner swap, and complete role swap;
  all emitted values match the authoritative parser, canonical serializer, and
  definition hash.
- The final compiler/test blobs are `95bb7581…251a` and `d796545f…3113`.
  The corrected 35-test boundary passes in 857.518 seconds. An independent
  review first exposed dynamic instance-method shadowing at all four outer
  layers; the repaired boundary preflights every nested typed object and rejects
  class, method, and payload-carrier replacement. Two final read-only reviews
  report P0=P1=P2=P3=0. The complete dependency-free suite passes 956/956 in
  3,085.160 seconds with the same two intended opt-in skips.
- Slice-3's pre-implementation contract is now closed independently. Exact
  initial goal and legal-action formulas match the authoritative semantics;
  optimistic spatial support includes opponent PUSH/SWAP displacement and
  per-target ELIMINATE lineages; the resource-aware 55-state count lattice keeps
  A-first and B-first separate through ply 18. A weaker role-local reachability
  proposal was rejected before implementation because it can falsely exclude an
  opponent-assisted connection.
- The exact skeleton stabilizers are `{I}:141`, reflection subgroups 810/165,
  axis-Klein 396, and full D4 6. Independent Burnside and ordered-population
  derivations both recover 5,111,055 setup carriers, global orbit weights
  1,184,850/3,294,033/627,732/4,440, weighted setup mass 10,319,364, and paired
  member mass 20,638,728. These are now sealed design and implementation
  authorities, but remain non-production arithmetic rather than a completed
  census result.
- Plan-0015 slice 3's one-carrier `initial_structure` kernel is complete. It
  derives exact initial goals, legal/dependency counts, pair-aware optimistic
  support, the resource-aware two-tempo count proof, separated descriptor
  groups, complete structured reasons, and a strict output-only prepared
  snapshot without transition or outcome capability. The fixed source/test
  blobs are `c7d806aa38c3a8152ae6289f64e3cba0765a7ac1` and
  `18ff527411db6fc1669c68358f8435425ed7287b`; the focused boundary passes 30/30
  in 9.618 seconds, and the complete dependency-free suite passes 989/989 in
  4,357.623 seconds with the same two intended opt-in skips. Independent final
  semantic and boundary audits report P0=P1=P2=P3=0.
- Plan-0015 slice 3's factorized `static_census` implementation is complete at
  source/test blobs `fd5fb486ea33fac840b2951ff0a3680a3c221058` and
  `93aa6d428b5378f06027438c04d6a38f9ad25bae`. Its canonical 6,798-row setup
  table has root
  `f1bc82d1cfbaac9933e097aba2f4118737e19f7a190f06bc9f0a83add17c2e23`,
  and its exact 1,518-skeleton authority has root
  `53221ffdbd42e4c86e9c54eadb6e0659bf71ff350dcdf0944b062ce56c3e1488`.
- Independent implementation paths reproduce fixed-point counts
  6,798/2/62/2/224/224/224/224, the five setup-orbit counts
  6,798/3,511/3,511/1,827/970, skeleton stabilizers 141/810/165/396/6,
  global weights 1,184,850/3,294,033/627,732/4,440, and the exact populations
  5,111,055 factorized carriers, 10,319,364 labeled setups, and 20,638,728
  paired members.
- The production one-shot builder performs exactly 1,518 shard derivations in
  one carrier pass and retains no full-domain carrier set. The incremental
  checkpoint API deliberately revalidates prefixes and is diagnostic rather
  than the production path. Leaf, shard, group, checkpoint, and report values
  are strict, detached, domain-separated, and output-only.
- The dedicated 36-test static-census boundary passes in 601.045 seconds. The
  complete dependency-free suite passes 1,025/1,025 in 4,908.329 seconds with
  two intended opt-in skips. Two independent final read-only reviews report
  P0=P1=P2=P3=0 after closing helper-binding, embedded-identity, standalone-
  shard, boolean/numeric-alias, and nested-tuple boundary findings.
- The production evidence contract is preregistered before implementation. It
  fixes one noninjectable no-argument builder call after durable reservation and
  attempt, a 256 MiB canonical report artifact, compact reconstruction from all
  1,518 shard summaries, three current-source reseals, and an exclusive
  bootstrap/reservation/attempt/report/body/terminal lifecycle. Recovery never
  resumes or repeats the census, and mutable checkpoints are not evidence.
- The authorized Plan-0015 static census ran exactly once and sealed
  `COMPLETED`. Public recovery returned `VERIFIED_NO_OP`, and independent stored-
  report reconstruction matched the 17,284,867-byte artifact at SHA-256
  `fa7daf48992f2b186fd980d42f670beda651b1adc7fe4c1fb62b67d1fdc05f97`.
  The terminal seal is `299a76910ee7fa072c0ec1b8cadb31d13714b922d0dcfa7c65c06e96e2306e34`.
- The census covers 5,111,055 factorized carriers, 10,319,364 labeled setups,
  and 20,638,728 paired first-player members. Exactly 880,986 representatives
  and weighted mass 2,025,808 pass the preregistered static gates; 846,340 of
  those representatives are `CONTACT` and 34,646 are `SEPARATED`. These are
  structural eligibility counts only, not candidate or quality evidence.
- The Plan-0015 history cutoff is fixed at commit/tree `0d041629b…fee299` /
  `4adb84cf…f4853`. Its 152,749 experiment JSON paths close exactly as 66 prior
  sources, 152,678 paths under the Plan-0013 evidence tree `40f4b65f…993e489`,
  and five paths under the definition-free Plan-0014 tree
  `beaed7c4…03f96`. The latter two trees can be authenticated and classified
  without opening candidate, journal, trace, or outcome artifacts.
- The pure `history_cutoff` firewall now validates that complete path sequence,
  its domain-separated path roots, all four Git coordinates, exact lexical
  ordering, and the 66 + 152,678 + 5 classification without filesystem, process,
  network, dynamic-import, legacy-package, or artifact-content capability. Its
  focused boundary passes 6/6 tests, including independent Git-object checks;
  the complete suite passes 1,150 tests with two intentional skips.
- The cutoff-gated `history_identity` adapter now authenticates exactly the 66
  legacy blobs and seven Plan-0012 fixtures, then emits only exact, D4,
  complete-role-swap/global-piece-alpha, schema-version, and source-carrier
  identities. Its 2,250
  definition occurrences close as 1,179 exact, 1,175 D4, and 1,175
  role-neutral identities across 73 carriers, with one retained malformed count.
  Carrier, identity, and complete projection roots are `51b7416d…2c40f`,
  `e26fbd0d…d26b93`, and `00973dcc…e2e3c9`. All 33 focused cutoff, identity,
  wire-compatibility, and capability tests pass. Independent final review found
  no unresolved P0–P3 defect. The repaired closure uses only the standard
  library; it cannot import the legacy package/engine. All 1,179 definitions
  match independent exact, D4, and role-neutral oracles. Neither Plan-0013 nor
  Plan-0014 evidence members are opened. Full-suite acceptance remains pending.
- The source-only additional Plan-0013 synthetic fixture is separately pinned
  and proven outside the new compiler image by its two-ply horizon and
  non-isotropic SWAP vectors. It is not silently added as an eighth fixture to
  the frozen 73-carrier projection.
- Public compact reconstruction and independent grouping of the sealed census
  prove nine of 109 semantic classes and 51 of 1,518 skeletons have zero
  eligible supply. Across 3,036 strata, 704 have zero, 81 have one to four,
  and 2,251 have at least five before history exclusion. Since fresh supply
  cannot exceed eligible supply, the registered breadth floor is impossible.
  The [negative report](../experiments/reports/0015-static-breadth-floor-negative.md)
  records the proof, counts, source hashes, and read-only reproduction command.

## What is not yet trustworthy

- No candidate fairness, strategic richness, or enjoyment claim is trustworthy.
- Cascade-v2 admission is not a trustworthy discovery policy: it selected ply-limit
  censoring and routed only candidates that were already unable to survive.
- General depth-5 draw handling remains formally inconclusive: the enriched set
  contained only 13 cases, below the preregistered minimum of 15.
- Every one of the 13 landscape draws ended at a six- or nine-ply cap and every
  strong profile was 30/30 ply-limit draws with excessive-draw and long-game
  failures; none is a candidate.
- The equal-stratum landscape is descriptive and cannot estimate generator-v2
  prevalence or prove unsampled D4 orbits empty.
- Production exact evidence remains restricted to 3×3. Plan 0011's preserved
  4×4 exact and depth-5 evaluators were never run on a production manifest, so
  evaluator transfer remains unknown rather than negative.
- Provisional complexity and play-shape limits have no human calibration.
- Goal-directed-v1 has a strong B-role bias and is not a monotone strength proxy.
- General depth-5 handling of stalemate draws is still inconclusive at 2 completed
  cases, despite complete negative frontier coverage.
- The raw stalemate artifact attests the replay gate through frozen control flow but
  does not retain candidate-level fresh-replay evidence or its digest.
- Plan 0008's formal capture frontier is `INCONCLUSIVE`: 88 cases were stress-
  eligible, the frozen cap selected 32, and 56 remain intentionally untested.
  `ROLE_DIVERSE` is descriptive only and cannot support adoption.
- Every strongly evaluated capture case was internally role-dominant. No
  candidate-shaped capture evidence exists.
- The three-versus-four-vector capture boundary is now explicitly unsupported;
  unrestricted capture and a capture-aware generator are not trustworthy next
  discovery directions.
- The two-runner setup is not a trustworthy discovery substrate: mechanical
  engagement was broad, but every complete strong-frontier case was role-dominant.
- General 4×4 evaluator accuracy is not trustworthy. Prior 4×4 depth-5 cases had
  no exact labels and outcome-sensitive admission. The preserved max-8
  definition-only census, selector, pure exact-evidence validator, and pure fixed
  depth-5 validator were deliberately left unspent.
- The Plan-0012 feasibility census is trustworthy only as initial-position and
  structural evidence. It does not prove that a CONNECT goal is dynamically
  reachable, that a legal action is consequential, that families have equal
  difficulty, or that any game is fair, strategic, or enjoyable.
- The final registry, search envelope, paired universe, history projection, and
  selected development/candidate membership remain outcome-free structural
  evidence. The family-neutral evaluator is trustworthy only against its frozen
  synthetic calculation benchmark; its accuracy and play shape on selected atlas
  games are unknown. The immutable manifest, exact labels, both weak-play
  profiles, and descriptive telemetry now exist, but no confirmation allocation
  or human-play evidence exists. Replay telemetry remains descriptive rather
  than a calibrated strategic-quality or enjoyment threshold.
- No Plan-0013 completed assessment body exists. The sealed Plan-0014
  attestation is a post-failure mechanical reconstruction and must not be
  presented as completing V2 or as confirmation. All 14 frontier pairs are
  exact first-player-dominant, so they are matched two-game role-swap signals
  rather than fair single games, strategic-depth evidence, enjoyable games, or
  confirmation candidates.
- Plan-0015's static census is complete and reconstructable; its negative
  breadth decision does not imply that the 100 nonempty classes lack fair or
  enjoyable games. No partition was executed. Skipping partition is a recorded
  execution decision after the monotone necessary-condition proof, not a claim
  that the original partition definition of done was performed.
- Static eligibility in Plan 0015 proves only that a carrier is structurally
  admissible for a later probe. It cannot establish dynamic reachability,
  fairness, strategy, replay value, or fun.

## Current blockers

No human or resource blocker. The static census is terminal and verified, and
the scientific breadth result is negative. The repaired history code's full
regression is still running; do not mark the implementation accepted or move the
active plan until its result is known. Durable log:
`/tmp/parity-forge-history-tests.ku8hkt/full-suite.log` (started with
`PYTHONPATH=src python3 -m unittest discover -s tests -v`). The focused 33-test
result and final independent review are already clean.

A same-task continuation heartbeat named `Parity Forge 研究継続` (automation ID
`parity-forge`) is active every 30 minutes. It first checks this existing test
log without starting a duplicate run, then completes the closeout and advances
only a reviewed, separately registered next scope. Unchanged waits stay quiet.
Local continuation requires the computer and Codex app to remain running.

## Most important unknown

Which general, preregistered development experiment can test gameplay-unseen
structural breadth without confusing static supply with game quality or
relabeling Plan 0015's negative result. New primitive implementation is not the
default next step.

## Next action

Finish the existing full regression, then archive Plan 0015 with scientific
decision `BREADTH_FLOOR_NOT_MET` and partition `NOT_STARTED`. Open the next
definition-only scope/coverage protocol as a separate plan; do not rerun the census,
select from the 100 remaining classes, or execute gameplay.

## Next human decision, if any

None.

## Recent experiments

- `20260830T154309225370Z-batch-g20260831`: generator v2, 57/100 reached play,
  zero survivors.
- `20260830T154446539528Z-audit-374bfc58`: 20 exact cases; 11 A, 7 B, 2 draws.
- `20260830T154733530838Z-calibrate-f27833e8`: depth 5 matched 20/20.
- `20260830T184008717197Z-strong-g20260901`: 99 novel exact-hash definitions;
  26/63 cheap-play admissions, 14/14 exact and 12/12 depth-5 completion, zero
  survivors; primary result inconclusive because there were no exact draws.
- `20260830T184737002247Z-blind-depth5-cdf26f22`: depth 5 matched 14/14 held-out
  forced directions with no deferral; predeclared result inconclusive at n=14.
- `20260830T200230881318Z-landscape-f407aefb`: exact 384/384, A/B/draw
  256/115/13, no censor; all draws ended at `PLY_LIMIT`.
- `20260830T200513661140Z-draw-stress-1edf571b`: all 13 draws completed depth 5,
  all remained non-decisive but failed strong duration/draw shape; frontier zero.
- `20260830T214033891685Z-stalemate-ed9a9b93`: exact 128/128, A/B/draw
  73/35/20, no censor or horizon; two static-valid draws, none shape-clean.
- `20260830T214159802995Z-stalemate-stress-239ad79d`: both eligible draws
  completed depth 5 as 30/30 stalemates; frontier zero, overall not supported.
- `20260830T225920216640Z-capture-6bc46645`: paired exact 128/128; source A/B
  93/35, treatment 32/96, 61 A-to-B changes, no state censor or horizon result.
- `20260830T230308952280Z-capture-stress-e4fc6e97`: 32/32 fixed selected cases
  completed depth 5; exact A/B 3/29, all 30–0 role-dominant, but 56 eligible cases
  were outside the preregistered cap and the formal result stayed inconclusive.
- `20260831T012026857459Z-capture-boundary-exact-e1b0f6a7`: paired exact 128/128;
  source A/B 49/15, treatment 6/58, no censor or horizon; v3 and v4 both 3/29.
- `20260831T012251133134Z-capture-boundary-depth5-cbf2ccc3`: 128/128 profiles and
  3,840 games completed; frontier A/B 6/46, candidate-shaped zero, formal result
  `NOT_SUPPORTED_VECTOR_BOUNDARY` with branch `FAMILY_REASSESSMENT`.
- `20260831T042721207815Z-two-runner-exact-3b7757ad`: paired exact 128/128;
  source A/B 35/29, treatment A/B 34/30, one A-to-B change, no censor or horizon.
- `20260831T043600138223Z-two-runner-depth5-2353bfb8`: 128/128 profiles and
  3,840 games completed; 46-case engaged frontier A/B 24/22, all role-dominant,
  candidate-shaped zero, formal result `NOT_SUPPORTED_TWO_RUNNER_SETUP`.

## Current best candidates

None. The 15 schema-v1 exact draws are horizon diagnostics; the two static-valid
schema-v2 natural draws are short all-draw obstruction cases; all 96 strongly
evaluated schema-v3 profiles across Plans 0008–0009 are role-dominant; and all 46
Plan-0010 two-runner frontier profiles are likewise role-dominant. Plan 0014
formally reconstructs 14 Plan-0013 frontier pairs, but every one is exact
first-player-dominant. Plan-0015's slice-2 wire definitions and Slice-3 static-
census implementation remain outcome-free calculation machinery and are not
candidates.

## Risks

- Repeated capture and replacement produced long exact motifs even without a
  `PLY_LIMIT`; horizon outcomes must remain diagnostics rather than successful
  draws.
- The fixed six-cell portfolio can still hide combinatorial expansion during
  play. Its signatures, setup domain, state/work bounds, strata, and quotas are
  closed, but exact and sampled work must still retain deterministic caps and
  censoring rather than treating structural bounds as expected runtime.
- Apparent branching can immediately reconverge, and apparent board contact can
  be unilateral. Synthetic telemetry can verify calculation and counterexamples,
  but cannot validate a universal agency or fun threshold; the first atlas must
  treat it descriptively.
- Reusing `MOVE_CAPTURE` in the provisional portfolio could be misread as
  reviving the rejected schema-v3 runner treatment. The complete ordered family
  signature, setup, source membership, and evidence must remain separate; an
  action rename cannot manufacture novelty.
- Atlas membership is now frozen from outcome-free evidence. Any later attempt to
  replace a difficult, censored, dominant, or uninteresting selected member—or
  to consume the candidate block during development—would invalidate the atlas.
- Equal case quotas do not imply equal difficulty across heterogeneous branching
  and state spaces. A later atlas must report work bounds, actual node use, D4
  orientation policy, and censor-aware family results separately.
- Early metrics may reward stalemate exploitation, triviality, or evaluator
  weakness.
- The broader occupancy vocabulary may still lack any experientially fair
  candidate; a negative pilot would apply only to its frozen signatures and
  setup envelopes, not to individual-ludeme causality, arbitrary occupancy games,
  or abstract games generally.
- Role-swap canonicalization can silently double-count the same mechanics or
  erase a genuinely directed goal relation if its D4 witness is wrong. Plan 0015
  therefore requires a complete inverse witness and an independent Burnside
  derivation before any setup census.
- A universe with millions of static carriers can still contain no good game.
  Plan 0015 must stream and partition it without treating size, eligibility,
  contact, branching, or complexity as evidence of quality.
- The minimum paired stratum has 12 eligible D4 setup pairs and is now partitioned
  into two development, two unevaluated candidate-block, and eight untouched
  pairs. That partition cannot be replenished, backfilled, or reordered after
  seeing an evaluator or gameplay outcome.

## Deferred ideas

Resuming the unspent Plan-0011 4×4 evaluator calibration; natural-horizon 4×4 or
5×5 family viability; additional or compound ludemes outside schema v4;
generator reweighting inside a confirmed family; evolutionary search,
compression, novelty archives, role-specialized agents, UI, cloud compute,
reinforcement learning, arbitrary mechanics, and external services.
